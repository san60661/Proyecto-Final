<!-- <table class="table"> 
		<tr>
			<th>NOMBRE</th>
			<th>DESCRIPCION</th>
		</tr>
	</thead>
	<tbody>
		<% @residences.each do |r| %>
			<tr>
				<td><%= r.name %></td>
				<td><%= r.description %></td>
				<td><%=link_to 'editar', edit_residence_path(r)%></td>
				<td><%=link_to 'eliminar', r, method: :delete%></td> 
			</tr>
		<% end %>
	</tbody>
</table>

<%= link_to 'Nueva residencia', new_residence_path %> --> 

<h2>Residencias</h2>

<table class="table">
	<thead>
		<tr>
			<th width="80%"> Nombre de la residencia </th>
			<th width="20%"> Lugar </th>
			<th colspan="2"></th>
		</tr>
	</thead>

<tbody>
	<% @residences.each do |residence| %>
	<tr>
		<td><%=residence.name %> </td>
		<td><%=residence.description %></td>
		<td><%=link_to 'Editar',edit_residence_path(residence), class: 'btn btn-info btn-sm' %></td>
		<td><%=link_to 'Eliminar', residence, method: :delete, class: 'btn btn-danger btn-sm', data: { confirm: '¿Realmente querés eliminar la residencia?'} %></td> 
	</tr>
	<% end %>
</tbody>
</table>
<%= link_to 'Nueva residencia', new_residence_path, class: 'btn btn-primary' %>
<br>